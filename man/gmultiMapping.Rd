% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmulti.R
\name{gmultiMapping}
\alias{gmultiMapping}
\alias{gmultiMapping,giottoMulti-method}
\alias{gmultiMapping<-}
\alias{gmultiMapping<-,giottoMulti-method}
\title{gmulti federation mapping accessor}
\usage{
gmultiMapping(x, ...)

\S4method{gmultiMapping}{giottoMulti}(x, which = NULL, ...)

gmultiMapping(x, ...) <- value

\S4method{gmultiMapping}{giottoMulti}(x, ...) <- value
}
\arguments{
\item{x}{a \code{giottoMulti}}

\item{...}{see \code{which} and \code{handle}}

\item{which}{one of \code{"spat_unit"}, \code{"feat_type"} or \code{"values"} to
narrow the return / target axis; default \code{NULL} returns or replaces
the full mapping list}

\item{value}{depends on the setter form: full mapping list,
axis-shaped list, single per-sample-named char vector, or \code{NULL}}

\item{handle}{a single gmulti-level handle name (e.g. \code{"cell"}) under
the chosen axis; used only by the single-entry setter}
}
\value{
the requested mapping (full list or one axis)
}
\description{
Get or set the \verb{@mapping} slot: declares which child-level spat_units,
feat_types and expression names (\code{values}) federate up to gmulti-level
handles, with per-child name reconciliation.

Auto-discovered at construction: \code{spat_unit} / \code{feat_type} get the
symmetric trivial mapping (handle == child-level name), and \code{values} is
seeded to the ingest convention \code{"raw"}. Every entry keys \strong{every}
sample; \code{NA_character_} is the deliberate-skip sentinel ("this sample
does not contribute to this handle"). A keyed sample whose child cannot
satisfy the entry is a loud error at read time, naming the sample.

The setter has three forms:
\itemize{
\item Full replacement: \code{gmultiMapping(mg) <- list(spat_unit = ..., feat_type = ..., values = ...)}
\item Whole-axis replacement: \code{gmultiMapping(mg, "spat_unit") <- list(cell = ..., nucleus = ...)}
\item Single-entry replacement: \code{gmultiMapping(mg, "values", "raw") <- c(B191 = "raw", B215 = "counts")}
}

Setting a single-entry vector to \code{NULL} removes that entry. Assigning
the top-level mapping to \code{NULL} triggers fresh auto-discovery from the
current child population.

All setter forms validate the resulting mapping and invalidate joint
slot state per affected universe — joint state for unrelated universes
survives untouched. \strong{Expansion is blocked once a universe is
materialized}: joint content was built from a specific participation
set, so widening the declaration afterwards would leave content that
omits a sample the declaration now claims. Opting a sample in is
explicit — drop the joint content for that universe, then re-declare.
}
