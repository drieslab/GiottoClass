% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/NN_network.R
\name{createNetwork}
\alias{createNetwork}
\alias{createNetwork,matrix,kNNNetworkParam-method}
\alias{createNetwork,matrix,radiusNetworkParam-method}
\alias{createNetwork,matrix,sNNNetworkParam-method}
\alias{createNetwork,matrix,delaunayNetworkParam-method}
\alias{createNetwork,matrix,missing-method}
\alias{createNetwork,spatLocsObj,networkParam-method}
\alias{createNetwork,dimObj,networkParam-method}
\alias{createNetwork,giotto,NNNetworkParam-method}
\alias{createNetwork,giotto,radiusNetworkParam-method}
\alias{createNetwork,giotto,delaunayNetworkParam-method}
\title{Create a Network}
\usage{
createNetwork(x, param, ...)

\S4method{createNetwork}{matrix,kNNNetworkParam}(x, param, node_ids = NULL, verbose = NULL, backend = NULL, ...)

\S4method{createNetwork}{matrix,radiusNetworkParam}(x, param, node_ids = NULL, verbose = NULL, backend = NULL, ...)

\S4method{createNetwork}{matrix,sNNNetworkParam}(x, param, node_ids = NULL, verbose = NULL, backend = NULL, ...)

\S4method{createNetwork}{matrix,delaunayNetworkParam}(x, param, node_ids = NULL, verbose = NULL, backend = NULL, ...)

\S4method{createNetwork}{matrix,missing}(
  x,
  param,
  type = c("sNN", "kNN", "delaunay"),
  method = c("dbscan", "geometry", "RTriangle", "deldir"),
  node_ids = NULL,
  include_distance = TRUE,
  include_weight = TRUE,
  as.igraph = TRUE,
  verbose = NULL,
  backend = NULL,
  ...
)

\S4method{createNetwork}{spatLocsObj,networkParam}(x, param, node_ids = NULL, ...)

\S4method{createNetwork}{dimObj,networkParam}(x, param, dimensions_to_use = NULL, ...)

\S4method{createNetwork}{giotto,NNNetworkParam}(
  x,
  param,
  spat_unit = NULL,
  feat_type = NULL,
  space = c("expression", "spatial"),
  dim_reduction_to_use = "pca",
  dim_reduction_name = NULL,
  dimensions_to_use = seq_len(10L),
  spat_loc_name = "raw",
  ...
)

\S4method{createNetwork}{giotto,radiusNetworkParam}(x, param, space = c("spatial", "expression"), ...)

\S4method{createNetwork}{giotto,delaunayNetworkParam}(x, param, spat_unit = NULL, spat_loc_name = "raw", ...)
}
\arguments{
\item{x}{a data object (matrix, \link[=spatLocsObj-class]{spatLocsObj}, \link[=dimObj-class]{dimObj},
\link[=giotto-class]{giotto}, or a GiottoDisk \code{fileStore})}

\item{param}{a \link[=networkParam-class]{networkParam}-inheriting object}

\item{...}{additional arguments, for use in specific methods}

\item{dimensions_to_use}{integer vector; columns of the \code{dimObj} matrix
to keep when building the network. \code{NULL} (default) keeps all.}

\item{spat_unit}{spatial unit (\code{giotto} method)}

\item{feat_type}{feature type (\code{giotto} method, NN networks)}

\item{space}{for NN networks on a \code{giotto} object: which space the
neighborhood is defined in. Default \code{"expression"} (pulls a
dimension reduction such as PCA). Set to \code{"spatial"} to build a
spatial kNN/sNN from cell coordinates.}

\item{dim_reduction_to_use}{name of the reduction family to pull from the
\code{giotto} object (default \code{"pca"}). Only used when \code{space = "expression"}.}

\item{dim_reduction_name}{specific reduction name. Only used when
\code{space = "expression"}.}

\item{spat_loc_name}{spatial-locations name. Used by the Delaunay method
and by NN methods when \code{space = "spatial"}.}
}
\value{
A network. Concrete type depends on the Param's \code{output} slot
and any supplied \code{backend}: \code{"data.table"} of edges, \code{igraph}, or a
GiottoDisk \code{parquetEdgeStore}.
}
\description{
Generic for constructing a network (graph) from
coordinates, features, or an embedding. Methods dispatch on the input
data class and a \link[=networkParam-class]{networkParam}-inheriting object that selects
the algorithm (kNN, sNN, Delaunay, ...). Part of the \verb{create<Noun>}
object-construction family. Distinct from analysis-stage operations
such as \code{\link[=processData]{processData()}}, \code{\link[=filterData]{filterData()}}, \code{\link[=reduceData]{reduceData()}}, and
\code{\link[=analyzeData]{analyzeData()}}.
}
\details{
\code{radiusNetworkParam} defaults to \code{space = "spatial"} rather than
\code{"expression"}. A radius is a distance with units, and for a radius graph
those units are almost always the tissue's -- "cells within 25 um". Reaching
it through the general NN method would have silently measured \code{eps} in PCA
units instead. Pass \code{space = "expression"} to get the PCA-space behaviour
back.
}
