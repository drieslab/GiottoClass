% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-view.R, R/methods-view.R
\docType{class}
\name{giottoView-class}
\alias{giottoView-class}
\alias{giottoView,missing,missing-method}
\title{S4 giottoView class}
\usage{
\S4method{giottoView}{missing,missing}(gobject, name, space = NA_character_, ...)
}
\value{
\code{giottoView}
}
\description{
A \code{giottoView} is a composable, read-only, lazy subset recipe
over a \code{giotto} (or \code{giottoMulti}) object. It records predicate filters,
region crops, and sample selectors. Spatial transforms (positioning) live
on \link[=giottoSpace-class]{giottoSpace}, not here.

At resolution time, the recorded steps are applied against the current
gobject state without mutating it. Slotted views (added via
\code{giottoView(g, "name") <- v}) travel with the gobject through save/load.

Compose by piping through \code{subset()}, \code{crop()}, \code{selectSamples()}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{v <- giottoView(space = "atlas") |>
    subset(cluster == "A") |>
    crop(c(0, 100, 0, 100))

giottoView(g, "tumor_focus") <- v
}\if{html}{\out{</div>}}
}
\section{Slots}{

\describe{
\item{\code{steps}}{\code{list} of steps. Each step is a plain tagged list
(\code{list(type = "filter" | "crop" | "samples", ...)}) so a recipe is
inspectable, hand-editable, and survives serialization — see the notes
at the top of \code{R/classes-view.R}.}

\item{\code{space}}{\code{character(1)}. Optional reference to a slotted space name
(see \link[=giottoSpace-class]{giottoSpace}). The coordinate frame in which crop regions
are interpreted. \code{NA_character_} means the gobject's native frame.}

\item{\code{name}}{\code{character(1)}. \code{NA_character_} until the view is slotted into
a gobject; thereafter holds the slot key.}

\item{\code{source}}{\code{ANY}. Reserved pointer / fingerprint. \code{NULL} for standalone.}

\item{\code{misc}}{\code{list}. Provenance, cache keys, version stamps.}
}}

\examples{
giottoView()
}
