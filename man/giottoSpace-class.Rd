% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-space.R
\docType{class}
\name{giottoSpace-class}
\alias{giottoSpace-class}
\title{S4 giottoSpace class}
\value{
\code{giottoSpace}
}
\description{
A \code{giottoSpace} is a composable, opt-in coordinate-frame
recipe over a \code{giotto} (single-sample) or \code{giottoMulti} (multi-sample)
object. It records a sequence of spatial transform steps keyed by sample
name; consumer functions opt into a named space via \code{space = "name"} and
the recorded transforms are applied to position the data in that frame.

Transforms in a space are SAMPLE-UNIFORM — within a single sample, all
spatial elements move together. Per-element overrides are not supported;
use \code{\link[=materialize]{materialize()}} and per-element transforms post-hoc for that.

Compose with \code{+} to combine per-sample recipes into a multi-sample space:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{sa <- giottoSpace("sample_a") |> affine(M_a)
sb <- giottoSpace("sample_b") |> affine(M_b)
atlas <- sa + sb
giottoSpace(mg, "atlas") <- atlas
}\if{html}{\out{</div>}}

Same-sample composition concatenates steps in order:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{s <- (giottoSpace("sample_a") |> spin(30)) +
     (giottoSpace("sample_a") |> spatShift(dx = 10))
}\if{html}{\out{</div>}}
}
\section{Slots}{

\describe{
\item{\code{samples}}{named \code{list}. Keys are sample names (\verb{:default:} for
single-giotto context); values are lists of transform steps to apply in
order. Each step is a plain tagged list
(\code{list(type = "transform", op = , args = )}) — see the notes at the top
of \code{R/classes-view.R} for why steps are not S4.}

\item{\code{name}}{\code{character(1)}. \code{NA_character_} until slotted into a gobject.}

\item{\code{source}}{\code{ANY}. Reserved pointer / fingerprint. \code{NULL} for standalone.}

\item{\code{misc}}{\code{list}. Provenance, cache keys.}
}}

\examples{
giottoSpace()
giottoSpace("sample_a")
}
