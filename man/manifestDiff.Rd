% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest-diff.R
\name{manifestDiff}
\alias{manifestDiff}
\title{Diff two giotto manifests}
\usage{
manifestDiff(before, after)
}
\arguments{
\item{before}{\code{gmanifest} or \code{NULL} (treated as "object did not exist")}

\item{after}{\code{gmanifest}}
}
\value{
list with \code{changed} (logical), \code{summary} (character scalar) and
\code{detail} (added / removed / modified / object)
}
\description{
Compare two manifests from \code{\link[=objManifest]{objManifest()}} and report what changed, both as
data and as a single human-readable sentence. Pure: it reads manifests only,
never a \code{giotto} object.

This is what an execution tool attaches to each result so a model, a critic
or a scorer knows what an operation actually did, rather than inferring it
from printed console output.
}
\examples{
g <- GiottoData::loadGiottoMini("visium")

before <- objManifest(g)
g <- subsetGiotto(g, cell_ids = head(spatIDs(g), 100))
manifestDiff(before, objManifest(g))$summary
}
