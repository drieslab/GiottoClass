% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-binpoints.R, R/methods-crop.R
\name{crop,giottoBinPoints,ANY-method}
\alias{crop,giottoBinPoints,ANY-method}
\alias{crop}
\alias{crop,giottoLargeImage,ANY-method}
\alias{crop,giottoAffineImage,ANY-method}
\alias{crop,spatLocsObj,ANY-method}
\alias{crop,giottoPoints,ANY-method}
\alias{crop,giottoPolygon,ANY-method}
\alias{crop,gAny,ANY-method}
\title{Crop to a spatial subset}
\usage{
\S4method{crop}{giottoBinPoints,ANY}(x, y, ext = FALSE, compact = "auto", ...)

\S4method{crop}{giottoLargeImage,ANY}(x, y, write = FALSE, filename = tempfile(fileext = ".tif"), ...)

\S4method{crop}{giottoAffineImage,ANY}(x, y, write = FALSE, filename = tempfile(fileext = ".tif"), ...)

\S4method{crop}{spatLocsObj,ANY}(x, y, ...)

\S4method{crop}{giottoPoints,ANY}(x, y, DT = TRUE, xmin = NULL, xmax = NULL, ymin = NULL, ymax = NULL, ...)

\S4method{crop}{giottoPolygon,ANY}(x, y, DT = TRUE, xmin = NULL, xmax = NULL, ymin = NULL, ymax = NULL, ...)

\S4method{crop}{gAny,ANY}(
  x,
  y,
  relation = "intersects",
  geom = c("centroid", "poly"),
  ...,
  view = NULL,
  space = NULL
)
}
\arguments{
\item{x}{object}

\item{y}{any object that has a SpatExtent or returns a SpatExtent}

\item{ext}{\code{logical}. When \code{TRUE}, the extent of y will be used instead of y}

\item{compact}{\code{character} or \code{logical} (default = "auto"). Whether to
compact object. See \link[=giottoBinPoints-class]{giottoBinPoints}. \code{"auto"} will perform a
compaction when number of spatial points referenced in \verb{@counts} is 1/10 of
that existing in \verb{@spatial}}

\item{\dots}{additional params to pass to terra::crop}

\item{write}{\code{logical} \if{html}{\out{<images only>}} (default = FALSE). Whether to write and
materialize the cropped subset to disk. This is overridden when a \code{filename}
is specifically provided.}

\item{filename}{\code{character} \if{html}{\out{<images only>}} (default is a .tif tempfile).
This file is not actually written unless the user specifically provides a
path or \code{write = TRUE}.}

\item{DT}{logical. Use alternative DT subsetting for crop operation}

\item{xmin, xmax, ymin, ymax}{only used if DT = TRUE. Set extent bounds
independently}

\item{relation}{\code{character(1)}. Spatial predicate. A crop narrows the
\strong{cell set}, so each cell is reduced to a geometry (see \code{geom}) and
tested against the region. One of \code{"intersects"} (default),
\code{"disjoint"}, \code{"within"}, \code{"touches"}, \code{"contains"}, \code{"covers"},
\code{"overlaps"}, \code{"crosses"}. The last four are always \code{FALSE} against a
centroid, so requesting one promotes \code{geom} to \code{"poly"} with a warning.
(\code{"covered_by"} is not a terra predicate and is rejected.)}

\item{geom}{\code{character(1)}. What represents a cell when the predicate is
evaluated: \code{"centroid"} (default) uses the cell's \code{spatial_locs} row —
cheap, and the conventional choice, but a cell whose polygon straddles
the region boundary with its centroid outside is dropped. \code{"poly"} uses
the cell's actual polygon — exact, and requires a polygon source on the
object. The choice is recorded on the step, so a saved recipe states
which question it asks.}

\item{view}{\code{NULL} or \code{character(1)}. When supplied, records the crop as
a step on the named view, creating it if it does not exist yet, instead
of executing eagerly. Eager \code{crop()} on a \code{giotto} is not yet
implemented.}

\item{space}{\code{NULL} or \code{character(1)}. Name of a slotted space on \code{x}.
Sets the view's \code{space} reference — the coordinate frame in which the
crop region is interpreted at resolution time. First call sets it;
subsequent calls that try to rebind to a different name error. \code{NULL}
leaves the view in whatever frame it was already bound to (or the
gobject's native frame if unbound).}
}
\value{
object of same class as \code{x}, spatially subsetted
}
\description{
Spatially subset an object x using object y. \code{crop()} will only return a
modified object if the \code{y} extent is smaller than the original in \code{x}.
For object type specifics, see below.
\subsection{\code{giottoLargeImage} and \code{giottoAffineImage}}{

When \code{write = FALSE} and a \code{filename} is not specified, \code{\link[terra:window]{terra::window()}}
will instead be used to create a lazy subset of the image. An independent
version of the cropped subset will only be created when either of the
above are provided, at which point it is handled through \code{\link[terra:crop]{terra::crop()}}.
\code{...} params are only used when materializing the subset.
}

\subsection{\code{giottoPoints} and \code{giottoPolygon}}{

An alternative faster crop operation is implemented through \code{data.table}
manipulation of the geometry information and are used by default. This mode
also only allows rectangular subsetting. Additionally, \code{giottoPolygons} will
be cropped using their centroids so that the entire polygon is either present
or not instead of the default \code{crop} behavior that would keep the portion of
the polygon that does fall within the selected crop region.
Set \code{DT = FALSE} in order to use the default style of terra::crop behavior
that also allows usage of additional params through ...
}
}
\examples{
g <- GiottoData::loadGiottoMini("visium")
img1 <- getGiottoImage(g, name = "image")
plot(img1)

# modify extent
e <- ext(img1)
e <- e - 1000

img2 <- crop(img1, e)
plot(img2)
}
