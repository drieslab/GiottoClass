% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{objManifest_json}
\alias{objManifest_json}
\title{Giotto object manifest as JSON}
\usage{
objManifest_json(x, file = NULL, pretty = TRUE, ...)
}
\arguments{
\item{x}{giotto object or a \code{gmanifest} from \code{\link[=objManifest]{objManifest()}}}

\item{file}{character. Optional path to write to. When \code{NULL} (default) the
JSON is returned as a character scalar.}

\item{pretty}{logical. Whether to indent the output}

\item{...}{additional params passed to \code{\link[=objManifest]{objManifest()}} when \code{x} is a
\code{giotto} object}
}
\value{
character scalar of JSON, or the file path, invisibly, when
\code{file} is given
}
\description{
Serialize the manifest from \code{\link[=objManifest]{objManifest()}}. Keys are emitted in canonical
(sorted) order and \code{NA}/\code{NaN}/\code{Inf} are encoded as the strings \code{"NA"},
\code{"NaN"}, \code{"Inf"} and \code{"-Inf"} by schema rule.
}
\examples{
g <- GiottoData::loadGiottoMini("visium")

cat(substr(objManifest_json(g), 1, 200))
}
