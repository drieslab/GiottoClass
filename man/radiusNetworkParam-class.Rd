% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/NN_network.R
\docType{class}
\name{radiusNetworkParam-class}
\alias{radiusNetworkParam-class}
\alias{radiusNetworkParam}
\title{Fixed-radius network parameters}
\usage{
radiusNetworkParam(
  eps,
  minimum_k = 0L,
  weight_fun = function(d) 1/(1 + d),
  include_weight = TRUE,
  include_distance = TRUE,
  output = c("auto", "data.table", "igraph", "parquet")
)
}
\arguments{
\item{eps}{numeric. Radius within which nodes are joined. There is no
default -- the right value is set by the data's units and cell spacing.
A reasonable starting point is a high quantile of the edge lengths of a
Delaunay network on the same points.}

\item{minimum_k}{integer. Retain this many nearest neighbours per node even
if they fall outside \code{eps}. Guards against isolated nodes in sparse
regions, which otherwise drop out of the network entirely. Default \code{0}.}

\item{weight_fun}{function mapping distance to weight}

\item{include_weight, include_distance}{logical}

\item{output}{one of \code{"auto"}, \code{"data.table"}, \code{"igraph"}, \code{"parquet"}}
}
\value{
a \code{radiusNetworkParam} object
}
\description{
Every pair of nodes within \code{eps} of each other is joined. Unlike kNN, node
degree is not fixed -- it follows local density, which is usually what is
meant by "cells that touch" in a tissue with varying cell density.

Previously the only way to get this was \code{kNN} with a large \code{k} and a
\code{maximum_distance} filter, which searches for neighbours you then throw
away. This searches for the ones you asked for.

The graph is symmetric by construction, so it is \strong{undirected}.
}
\section{Slots}{

\describe{
\item{\code{eps}}{numeric. radius; nodes closer than this are joined.}

\item{\code{minimum_k}}{integer. keep this many nearest neighbours per node even
when they lie beyond \code{eps}, so no node is left isolated. \code{0} disables.}

\item{\code{weight_fun}}{function. weight = \code{weight_fun(distance)}.}

\item{\code{include_weight,include_distance}}{logical. include columns.}

\item{\code{output}}{character. one of \code{"auto"}, \code{"data.table"}, \code{"igraph"},
\code{"parquet"}. See \code{\link[=createNetwork]{createNetwork()}}.}
}}

\section{Performance}{

Backed by \code{dbscan::frNN}, which is exact. At 200,000 points with mean degree
6 the search takes ~2 s. \code{spatstat.geom::closepairs()} is roughly 28x faster
and returns flat index vectors rather than the per-point lists \code{frNN} has to
be flattened out of, but \pkg{spatstat.geom} is not a Giotto dependency and
is 2D-only, so it is mentioned rather than used.
}

\examples{
p <- radiusNetworkParam(eps = 25)
}
