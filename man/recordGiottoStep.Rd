% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/function_logging.R
\name{recordGiottoStep}
\alias{recordGiottoStep}
\title{Record a giotto object history step}
\usage{
recordGiottoStep(
  gobject,
  fn = NA_character_,
  params = list(),
  status = c("ok", "error", "unattributed"),
  diff = NULL,
  error = NULL,
  description = NULL
)
}
\arguments{
\item{gobject}{giotto object}

\item{fn}{character. Name of the function or code that ran}

\item{params}{list. Parameters, as deparsed strings}

\item{status}{character. One of "ok", "error", "unattributed"}

\item{diff}{list. Manifest delta from \code{\link[=manifestDiff]{manifestDiff()}}}

\item{error}{character. Error message, when \code{status = "error"}}

\item{description}{character. Suffix for the step name}
}
\value{
giotto object
}
\description{
Append a history entry that \code{update_giotto_params()} cannot produce itself:
a call that failed, or a change made outside a logging function.

Only 52 of the suite's functions call \code{\link[=update_giotto_params]{update_giotto_params()}}, so a
manifest can move without any entry claiming it. An execution tool that
diffs \code{\link[=objManifest]{objManifest()}} before and after a chunk closes that gap by recording
the unclaimed change here with \code{status = "unattributed"}, rather than
letting it vanish.
}
\examples{
g <- GiottoData::loadGiottoMini("visium")

g <- recordGiottoStep(g, fn = "manual edit", status = "unattributed")
tail(names(objHistory(g)), 1)
}
