% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-view.R
\name{subset-view}
\alias{subset-view}
\alias{subset,giottoView-method}
\title{Subset (filter) a giottoView}
\usage{
\S4method{subset}{giottoView}(x, subset, ...)
}
\arguments{
\item{x}{a \code{giottoView}}

\item{subset}{an unevaluated predicate expression (captured via NSE)}

\item{...}{additional arguments forwarded to the underlying subset /
spatValues call at resolution time (\code{spat_unit}, \code{feat_type}, \code{negate},
\code{feat_ids}, \code{cell_ids}, ...)}
}
\value{
the view, with the filter step recorded
}
\description{
Record a predicate-style filter step on a \link[=giottoView-class]{giottoView}. The
predicate expression is captured unevaluated (NSE) at call site; at
resolution time it is evaluated against \verb{spatValues(g, feats = <names in predicate>)}, with any additional arguments (\code{spat_unit}, \code{feat_type},
\code{negate}, ...) forwarded through.

Values from the calling frame are substituted into the predicate at
record time, so \code{subset(v, cluster == target)} records the \emph{value} of
\code{target} rather than a reference to it — the recipe does not change if
\code{target} is later reassigned, and it survives serialization.

Cell-keyed propagation to other slots is automatic via cell_ID relations
— no flag needed.
}
\examples{
v <- subset(giottoView(), cluster == "A")
v
}
